[Unit]
Description=PostgreSQL VACUUM ANALYZE
BindsTo={{ postgresql_name }}.service
After={{ postgresql_name }}.service
Wants=postgres_vacuum.timer

[Service]
User=postgres
Group=postgres
Type=simple
ExecStart=/usr/bin/vacuumdb --quiet --analyze --all

[Install]
WantedBy=multi-user.target
